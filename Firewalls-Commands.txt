Commands Used

# Install UFW
sudo apt install ufw

# Check Firewall Status
sudo ufw status numbered

# Enable Firewall
sudo ufw enable

# Block Specific Port (Example: Telnet Port 23)
sudo ufw deny 23

# Allow SSH (Port 22)
sudo ufw allow 22

# Remove Test Block Rule
sudo ufw delete deny 23
